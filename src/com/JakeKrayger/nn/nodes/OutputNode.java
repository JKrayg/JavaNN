package src.com.JakeKrayger.nn.nodes;

import src.com.JakeKrayger.nn.components.*;

// OutputNode and HiddenNode are the same class
public class OutputNode extends Node {
    public OutputNode(double value, double bias) {
        super(value, bias);
    }
    
}
